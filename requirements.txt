# NOTE: OCR (pytesseract, pdf2image) also needs system packages pip can't install:
#   Ubuntu/Debian: sudo apt-get install tesseract-ocr poppler-utils
#   macOS:         brew install tesseract poppler
# Without these, pytesseract/pdf2image import fine but fail at runtime.
langchain==0.3.7
langchain-text-splitters==0.3.2
sentence-transformers==3.3.1
psycopg[binary]==3.2.3
pgvector==0.2.5
groq==1.6.0
fastapi==0.115.5
uvicorn[standard]==0.32.1
pdfplumber==0.11.4
pytesseract==0.3.13
pdf2image==1.17.0
python-dotenv==1.0.1
ragas==0.2.6
pydantic==2.10.2
